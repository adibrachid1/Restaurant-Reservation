<?xml version="1.0" encoding="utf-8"?>
<menu xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto">

    <item android:id="@+id/profile_button"
        android:icon="@drawable/ic_profile_icon"
        android:title="Profile"
        app:showAsAction="ifRoom|withText" />
    <item android:id="@+id/favorites_button"
        android:icon="@drawable/ic_favorites_icon"
        android:title="Favorites"
        app:showAsAction="ifRoom|withText" />
    <item android:id="@+id/preferences_button"
        android:icon="@drawable/ic_baseline_menu_24"
        android:title="Preferences"
        app:showAsAction="withText" />
    <item android:id="@+id/logout_button"
        android:icon="@drawable/ic_logout_icon"
        android:title="Logout"
        app:showAsAction="withText" />
        android:title="Logout"/>

</menu>